/// All game state for the Prince of Persia Rust port.
/// Fields are added one subsystem at a time as C files are ported.
/// Never remove a field once added.
pub struct State {
    // populated subsystem by subsystem
}
